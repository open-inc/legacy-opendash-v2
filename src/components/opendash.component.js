import template from './opendash.component.html';

class controller {

    constructor() {
    }
}

let component = {
    controller,
    template,
    bindings: {
    },
};

export default component;
